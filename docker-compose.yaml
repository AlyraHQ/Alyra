services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: Alyra
      POSTGRES_PASSWORD: trustOfGod
      POSTGRES_DB: alyra
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: